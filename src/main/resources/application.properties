
spring.webflux.base-path=/auth

# Datasource settings
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://nas01.local:3306/dev
spring.datasource.username=dev
spring.datasource.password=foobar
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=none

spring.liquibase.change-log=classpath:/db/changelog-master.xml

# JWT settings
authservice.issuer=MUSTOVERRIDE
authservice.secret=MUSTOVERRIDE
authservice.salt=MUSTOVERRIDE
authservice.jwtDuration=PT5M
authservice.refreshDuration=PT15M

